import { Heading, Box } from '@chakra-ui/react';
import { useTable, ReactT } from 'react-table';
export default function OrderTable({ orders }) {
  // const = useTable()
  return (
    <Box>
      <Heading>Order table</Heading>
    </Box>
  );
}
